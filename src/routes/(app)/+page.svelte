<script lang="ts">
/** Landing route: the loader redirects, so this is only ever a brief flash. */
import Skeleton from '$ui/primitives/Skeleton.svelte';
</script>

<div class="p-6"><Skeleton lines={3} /></div>
