<script lang="ts">
/**
 * Settings section: SettingsOverrides.
 *
 * The component owns loading and mutations; this route only mounts it inside the
 * settings layout.
 */
import SettingsOverrides from '$ui/settings/SettingsOverrides.svelte';
</script>

<svelte:head><title>Overrides · Mentat</title></svelte:head>

<SettingsOverrides />
